{
  "id": "bcb54b94",
  "title": "精简 IP-Sentinel 项目",
  "tags": [],
  "status": "done",
  "created_at": "2026-05-11T03:52:46.438Z"
}

所有精简工作已完成，变更摘要：

1. **删除 telemetry/** — 移除独立的 worker.js，减少仓库体积
2. **core/utils.sh** — 新增 `detect_curl_imp()` 和 `log_sentinel()`，删除 `generate_ua_list()`、`url_encode()`、`jitter_coord()` 三个死函数
3. **core/standalone_daemon.sh** — 使用 utils.sh 共享函数，curl 检测和日志不再重复；时区改为读取 config.conf 的 `UTC_OFFSET`
4. **core/mod_google_curl_imp.sh / mod_trust_curl_imp.sh** — 统一使用 `detect_curl_imp` 和 `log_sentinel`
5. **core/updater.sh** — 移除脚本顶层非法 `local` 关键字
6. **core/uninstall.sh** — 删除 `tg_daemon`、`agent_daemon` 等历史残留进程名
7. **install_standalone.sh** — 不再拉取已废弃的 `user_agents.txt` 和 `kw_*.txt`；新增 `UTC_OFFSET` 写入 config.conf；支持 Default 州的扁平化路径
8. **data/regions/** — 扁平化：JP/UK/DE/FR/SG/HK 的 `Default/` 层级已上移
9. **data/map.json** — 删除 `keyword_file` 字段，添加 `utc_offset`，版本号同步为 3.4.0
10. **data/regions/JP/Tokyo.json** — 修复 `"kakaku.com/"` 缺少 `https://` 前缀的 bug
11. **README.md** — 重写开发者文档，移除 telemetry 段落和所有已删除资源的引用
